---
- name: restart backend
  ansible.builtin.shell: docker compose restart
  args:
    chdir: /home/ubuntu/backend

# - name: rebuild backend
#   ansible.builtin.shell: docker compose down && docker compose build --no-cache && docker compose up -d
#   args:
#     chdir: /home/ubuntu/backend

# - name: reload backend config
#   ansible.builtin.shell: docker compose restart backend
#   args:
#     chdir: /home/ubuntu/backend