services:
  backend:
    build: .
    image: "{{ backend_image_name | default('backend-app') }}"
    container_name: "{{ backend_container_name | default('backend') }}"
    restart: unless-stopped
    ports:
      - "{{ backend_port | default(5000) }}:5000"
    env_file:
      - .env
