---
# Node Exporter systemd installation disabled
# Node Exporter is now managed by Docker Swarm via the stack compose file
# This avoids port conflicts and maintains centralized configuration

# The following tasks are commented out to prevent systemd installation
# Node Exporter will be deployed as a global service in Docker Swarm

# - name: Download node_exporter
#   get_url:
#     url: "https://github.com/prometheus/node_exporter/releases/download/v{{ node_exporter_version }}/node_exporter-{{ node_exporter_version }}.linux-amd64.tar.gz"
#     dest: /tmp/node_exporter.tar.gz

# - name: Extract node_exporter
#   unarchive:
#     src: /tmp/node_exporter.tar.gz
#     dest: /tmp
#     remote_src: yes

# - name: Install node_exporter binary
#   copy:
#     src: "/tmp/node_exporter-{{ node_exporter_version }}.linux-amd64/node_exporter"
#     dest: /usr/local/bin/node_exporter
#     mode: '0755'
#     remote_src: yes

# - name: Create node_exporter service
#   copy:
#     content: |
#       [Unit]
#       Description=Node Exporter
#       After=network.target

#       [Service]
#       User=nobody
#       ExecStart=/usr/local/bin/node_exporter
#       Restart=always

#       [Install]
#       WantedBy=multi-user.target
#     dest: /etc/systemd/system/node_exporter.service

# - name: Start node_exporter
#   systemd:
#     name: node_exporter
#     daemon_reload: yes
#     state: started
#     enabled: yes

- name: Node Exporter info
  debug:
    msg: "Node Exporter will be deployed via Docker Swarm stack - see compose-swarm.yml.j2"